dat= read.csv('bone.csv')
summary(dat)
m1 = lm(fnbmd ~ wt)
attach(dat)
m1 = lm(fnbmd ~ wt)
summary(m1)
anova(m1)
dat2= read.csv('Workbook2.csv')
attach(dat2)
m2 = lm(AveSpeed ~ Number)
summary(m2)
require(foreign)
require(nnet)
require(ggplot2)
require(reshape2)
ml <- read.dta("https://stats.idre.ucla.edu/stat/data/hsbdemo.dta")
str(ml)

with(ml, table(ses, prog))